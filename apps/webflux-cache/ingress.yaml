apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webflux-cache-ingress
  namespace: apps
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: api.blupine.freeddns.org
    http:
      paths:
      - path: /app-cache-1
        pathType: Prefix
        backend:
          service:
            name: webflux-cache-service
            port:
              number: 80